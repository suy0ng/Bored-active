import React from 'react';
import { Participant } from '../../types/Participant';

interface props extends Participant{}

const Result : React.FC<props> = ({participant}) => {
    return (
        <>
            
        </>
    );
};

export default Result;